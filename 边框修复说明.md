# 1.1 版边框小补丁

基于 GitHub 标签 `v1.1.0`（提交 `2f8a6c4`），保留 Windows 的 UTF-8 和文件替换重试修复。

## 另一台电脑怎样更新

1. 保存会话 JSON，停止工作台，备份下面三个旧文件。
2. 只替换 `workbench_core.py`、`workbench_export.py`、`workbench_web/app.js`。最后一个文件必须放在原来的子目录中。
3. 用原来的启动命令和原来的工作目录重新启动，不要加 `--demo`。
4. 浏览器按 `Ctrl+F5` 强制刷新，再导出。旧图片不会自动更新。

无需重装依赖，不改会话结构、选区、点大小或相机。

## 统一规则

- 网页原图和导出原图都使用「图像宽度 × 4/480」作为边框线宽。480 像素时为 4，1600 时约为 13.33；在相同显示宽度下比较，而非都以原始像素放大查看。
- 两边都以选区边界作为线条中心，不再网页居中、导出向内画。栅格化可能有不到一像素的舍入差异。
- 1.1 已调整过导出线宽比例；本补丁保留该比例，重点统一描边位置和局部预览。
- 局部图默认 `_roi_1` 等文件名现在就是带框版本，PDF/JPG/PNG 一致。`_roi_1_clean` 保留精确无框裁图，`_roi_1_framed` 保留兼容副本。
- 局部图的线条中心对齐原裁图边界，外加少量背景边距，避免外侧半条线被裁掉。原选区不变；新增边距记录在清单的 `image_offset` 中。
- 网页局部预览也按同一规则绘制，不再用固定粗细的网页外边框。预览与导出因分辨率不同仍有像素舍入差异。
- 原图最边缘处的描边超出画布部分仍会裁切；局部导出通过新增边距保留完整边框。

这是边框修复，不包含局部高清重渲染或补点。

## 验证

运行 `python -m unittest test_workbench -q`。测试覆盖不同分辨率线宽和居中位置、局部带框默认导出、无框裁图像素保持、会话恢复以及 PDF/JPG/PNG 导出流程。此测试使用替代渲染图，不依赖 GPU，不等同于 Windows 实机验证。
