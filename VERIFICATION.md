# Workbench 验收记录

## v2：实时预览与独立素材

2026-09-13 更新：

- 独立测试工作区实际执行拖动旋转、点大小滑块、删除框和撤销恢复。保留用户正在使用的工作区相机和框选。
- 实际工作区完成 1600 px 输出：3 个方法、每方法 2 个 ROI，共 18 组独立素材，即 18 个 PDF、18 个 JPG、18 个 PNG，另含透明框层、坐标 JSON、统一会话、manifest、素材索引和 ZIP；未生成默认 comparison。
- 6 张无框 ROI 的 PNG 与原图裁剪逐像素一致，所有 JPG 和 PNG 尺寸匹配，ZIP 校验通过。
- 通过 PDF skill 的 Poppler 回渲检查，独立带框 PDF 和局部 PDF 无拉伸、无标题或指标覆盖。
- 浏览器进度条从排队状态更新至 100%，完成后显示实际保存路径和素材/ZIP 链接。
- Python 回归测试 7 项通过；新增覆盖跨会话撤销、完整相机直接复用、独立 PDF/JPG/PNG 与进度步骤。`node test_live.cjs` 验证实时旋转保持焦点、距离和正交范围，输入相机不被修改。
- 实时预览每方法最多 20000 个采样点，导出使用全部原始点；没有承诺固定帧率，拖动中的材质和阴影不等同最终 VTK 效果。

## v1 历史验收

日期：2026-09-13。环境：macOS、本项目 `.venv`、PyVista/VTK 离屏渲染、浏览器本机回环访问。

## 已执行

- 真实浏览器打开工作台，加载三方法 / 三物体演示。
- 鼠标拖出矩形，在所有方法上同步框与局部预览。
- 像素对话框输入 `1600` 参考边长，选框 `(550,650,450,380)` 后正确显示。
- 切换物体后保存独立框；重启服务恢复会话和此前框选。
- 切换并排 / 图层叠加模式，修改方位角并刷新所有方法，保存 camera JSON。
- 从界面导出三行三列 comparison，同时输出 PNG/PDF。
- 实际检查 9 张单图均为 1600×1600；6 张局部图与干净原图对应区域逐像素相等。
- 使用保存的会话运行 `--export-only`，检查最终版 PDF 的 Poppler 回渲页面；局部边框贴合内容，方法列和局部框一致，没有拉伸或标签重叠。
- 浏览器控制台无应用错误；Python 编译和 JavaScript 语法检查通过。

## 回归测试

`python -m unittest test_workbench -v`：4 项通过。

1. 隐藏方法后，共享相机与基础半径保持不变。
2. 会话 / camera JSON 重开保留参数和框。
3. 局部裁图保留原始像素，导出中缺失方法保持原列位置。
4. 越界框在更改会话前被拒绝。

## 覆盖边界

实际渲染验收采用仓库的合成表面点云（稀疏 440 点、稠密 2200 点），不代表任何真实 baseline 的质量。没有据此声称已经验证所有 PU-GAN 对象、超大点云或 Linux 的每一种 GPU/VTK 后端。使用指南解释了统一坐标系、屏幕框随相机改变后的复核，以及拖动结束后刷新预览的交互方式。
